<%= link_to 'Return to previous page', users_path %></br>
<h1>Tables available at <%= @user.name %></h1>

<table>
  <tr>
    <th>Seater</th>
    <th>Availablilty</th>
  </tr>

<% @user.tables.each do |table| %>
  <tr>
      <td><%= table.seater %></td>
      <td><%= table.is_free %></td>
  </tr>
<% end %>
</table>
